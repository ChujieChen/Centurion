using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class handControlToCatapult : MonoBehaviour
{
    public bool underControlled;
    // Start is called before the first frame update
    void Start()
    {
        underControlled = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
